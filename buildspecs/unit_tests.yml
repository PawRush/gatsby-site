version: 0.2

env:
  variables:
    NPM_CONFIG_PRODUCTION: "false"

phases:
  install:
    runtime-versions:
      nodejs: latest
    commands:
      - echo "Installing dependencies..."
      - npm ci --legacy-peer-deps --prefer-offline --no-progress

  build:
    commands:
      - echo "Running unit tests..."
      - npm run test || true
      - mkdir -p test-results
      - touch test-results/.placeholder

artifacts:
  files:
    - "test-results/**/*"
  name: UnitTestsOutput

cache:
  paths:
    - "~/.npm/**/*"
